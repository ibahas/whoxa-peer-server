# PeerJS Server Configuration (Production)
PEERJS_PORT=4001
PEERJS_PATH=/
PEERJS_SECURE=true
PEERJS_HOST=your-domain.com

# CORS Configuration
# Add your allowed origins separated by commas
ALLOWED_ORIGINS=https://your-app-domain.com,https://api.your-app-domain.com

# Node Environment
NODE_ENV=production

# SSL Configuration (Required for HTTPS)
# Update these paths to your SSL certificate files
PEERJS_SSL_KEY=/path/to/ssl/key.pem
PEERJS_SSL_CERT=/path/to/ssl/cert.pem
